# Python
venv/
__pycache__/
*.pyc

# Local server
.server.pid

# Generated model data (large, regenerable via ./refresh-data.sh; CI rebuilds
# it fresh and ships it as the Pages artifact, so it's never committed).
data/

# Selenium test artifacts (large screenshots + logs, regenerable)
tests/auto/*.png
tests/auto/*.log

# LaTeX build artifacts (keep manual.tex, manual.pdf, figures/)
docs/*.aux
docs/*.log
docs/*.out
docs/*.toc

# macOS
.DS_Store
